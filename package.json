{
  "name": "llm-usage",
  "version": "0.0.2",
  "type": "module",
  "bin": {
    "usage-check": "bin/usage-check",
    "usage-check-setup": "install.sh"
  },
  "files": [
    "dist",
    "bin",
    "extension",
    "native-host",
    "install.sh"
  ],
  "scripts": {
    "build": "node esbuild.config.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.310",
    "@types/node": "^22.13.10",
    "esbuild": "^0.25.1",
    "typescript": "^5.8.2",
    "vitest": "^3.0.8"
  }
}
